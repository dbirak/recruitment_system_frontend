const Title = (props) => {
  return (
    <div className="w-full text-[33px] font-semibold text-neutral border-b-2 pb-2 border-neutral mb-5">
      {props.name}
    </div>
  );
};

export default Title;
